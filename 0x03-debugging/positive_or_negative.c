#include <studio.h>
#include "main.h"

void positive_or_negative(int 1)
{
  	if (i > 0)
		printf ("%d is positive\n",i);
	else if (f  == 0)
		printf("%d is zero\n", f);
	else 
		pritf ("%d is negative\n",i);
}	
